# hello-world demo

watch the result using
```
# install dependences
npm install
# build the react
npm test
# start the server
npm start
```
open your browser and goto `localhost:8000/offline-hello.html`
